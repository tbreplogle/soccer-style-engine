[pytest]
markers =
    quick: lightweight tests for everyday development.
    slow: full validation or workflow tests that are intentionally slower.
    integration: tests that exercise multiple subsystems together.
    real_data_optional: tests that may use optional real-data fixtures when present.
