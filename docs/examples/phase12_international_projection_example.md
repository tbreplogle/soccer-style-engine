# Phase 12 International Projection Example

This is an example format, not a generated report.

Matchup: Brazil vs Morocco  
Context: historical FIFA World Cup sample  
Neutral site: true

Example fields:

- `projection_profile`: `international_score_projection`
- `baseline_mode_used`: `opponent_adjusted_international_baseline`
- `data_mode`: `true_event_style_historical`
- `confidence_label`: usually Low or Medium unless both teams have strong prior samples
- `risk_flags`: sparse sample, historical-only data, roster uncertainty
- `international_context_warnings`: current live international data missing

Interpretation should stay conservative. Historical event data can help validate style context, but it is not current live team state.

