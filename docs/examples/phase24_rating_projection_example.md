# Phase 24 Rating Projection Example

Rating-only projection warning:

> This is a baseline score projection based on fixture + rating support only. It does not include current event data, xG, lineups, injuries, or style-aware matchup inputs yet.

The projection uses fixture + rating support to produce a conservative score baseline. It does not describe Elo as style, tactics, or a matchup advantage.

