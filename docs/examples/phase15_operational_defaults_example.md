# Phase 15 Operational Defaults Example

Example only.

`winner_probability` is the validated W/D/L context profile from Phase 14.

`score_projection` remains the general report view because it gives the clearest match-level projected score and xG-style report surface.

Confidence should be read as Data Support / Risk Context. It is not certainty and not a betting signal.

Proxy score adjustments are disabled by default. Free proxy style is not true event/tracking style.

No betting recommendation fields or pick language should appear in operational outputs.
