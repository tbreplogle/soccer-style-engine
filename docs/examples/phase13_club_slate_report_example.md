# Phase 13 Club Slate Report Example

This is an example output shape, not a generated report.

Slate type: `historical_validation_slate`

Columns include league, matchup, projection profile, xG, W/D/L probabilities, totals probabilities, confidence, risk flags, proxy context, market gap notes, and disabled betting recommendation.

The report should help compare projection profiles across a slate without implying picks.

