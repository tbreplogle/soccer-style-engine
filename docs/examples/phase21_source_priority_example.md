# Phase 21 Source Priority Example

Club projection stack:

```text
football_data -> clubelo -> understat -> fbref -> sofascore
```

Style/event proxy stack:

```text
whoscored -> sofascore -> fbref -> statsbomb_open_data
```

StatsBomb Open Data remains historical validation only.

