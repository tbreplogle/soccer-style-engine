# Phase 20 V1 Run Checklist Example

```powershell
.\scripts\test_quick.ps1
.\scripts\run_today.ps1
.\scripts\build_viewer.ps1
.\scripts\open_viewer.ps1
```

Expected shape:

```text
quick tests pass
run today status: success or success_with_warnings
viewer path: outputs\viewer\index.html
```

Warnings about local-only data, completed seasons, or missing optional raw data can be acceptable when understood.

