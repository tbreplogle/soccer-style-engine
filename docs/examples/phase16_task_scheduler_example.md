# Phase 16 Task Scheduler Example

Example only.

1. Review `scripts/run_daily_pipeline.ps1`.
2. Edit the default date, season, leagues, and currentness policy if needed.
3. Run `scripts/task_scheduler_example.ps1` to print the suggested task setup.
4. Uncomment the registration lines only after verifying paths and permissions.

Use `fail-on-unsafe` for routine automation. Use stricter policies when stale data should block the run.
