# Phase 19 Quick Test Example

Command:

```powershell
.\.venv\Scripts\python.exe -m pytest -m "not slow"
```

Purpose:

- everyday development loop
- skips slow validation suites
- still runs focused unit and workflow-polish tests

